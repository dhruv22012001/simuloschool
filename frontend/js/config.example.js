// Copy this file to config.js (gitignored) and point it at your API.
// Local backend:
window.API_BASE_URL = "http://localhost:8000";
// Prod (Render generates config.js at build time from the API_BASE_URL env
// var — see render.yaml — so this file is only for local dev):
// window.API_BASE_URL = "https://simuloschool-api.onrender.com";
